{
  "name": "@crewpoppy/lambdas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Lambda handlers CrewPoppy deploys into the owner's account (bundled by scripts/build-backend-bundle.mjs).",
  "main": "src/agent-runner.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
